# iTools 运营控制台 — 交接（第一 + 第二阶段）

> 状态：**两阶段均已完成、已上线、已验证**。
>
> | | 第一阶段（零服务端改动） | 第二阶段（服务端补丁） |
> |---|---|---|
> | 静态检查 | check / clippy 0 warning | 两端各 0 warning |
> | 单元测试 | 35 项 | 控制台 35 项 + 主服务 92 项 |
> | 集成测试 | — | 主服务 12 项（连真实 MariaDB） |
> | 本地端到端 | 174 项断言 | 62 项断言（两个服务一起跑） |
> | 生产验证 | 52 项断言 | 38 项断言 |
> | 真机核对 | 逐页截图 | 流量面板 + 停用全流程 |
>
> 上线地址：`https://<对外域名>:7005`（frp 透传，TLS 由本进程终结）。
> 真实域名见仓库根的 `.publish.env`（已 gitignore）——公开仓库不留运维拓扑。

---

## 一、任务与本次交付范围

海风哥的原话是「做一个后台管理系统，管理用户，禁用账号，插件管理，流量状况等等，部署到 nas 的服务器上」。

拍板的四个决策：
1. **独立管理员表 + 独立口令**（不复用 `SYNC_ADMIN_USERS` 白名单）
2. **独立静态站 + 独立端口**（7005）
3. **流量统计聚合落库**
4. 我做完整实现与部署脚本

开工后勘察出一个必须先讲清的事实，据此把范围切成两步：

**「禁用账号」与「流量统计」这两项，无论如何都要改云同步服务端的源码。**
- 禁用：`users` 表没有禁用位，且主服务的 `login`（`server/src/routes.rs:398`）只校验口令、
  `authenticate` 只查 `sessions`——控制台单方面加一列改一改是**彻底无效**的。
- 流量：主服务零指标采集，库里根本没有请求量/带宽/耗时数据，查不到不存在的东西。

海风哥当时明确「其它会话正在改服务端源码」，因此选择**分两步**：
第一阶段交付**零服务端改动**的部分（本文档），第二阶段再打服务端补丁。

### 第一阶段交付（零服务端改动）

| 模块 | 内容 |
|---|---|
| 概览 | 用户/会话/插件/存储汇总、用户增长曲线、提审状态分布、未采集指标清单 |
| 用户 | 列表（搜索/排序/分页）、详情（命名空间用量、会话、名下插件）、**强制下线**、**删除账号** |
| 插件市场 | 条目列表与详情（含作者/维护者下架区分）、提审历史。**只读** |
| 提审单 | 全量列表（按状态筛选）、详情（模型裁决原文 + plugin.json）。**只读** |
| 数据统计 | 5 条趋势曲线（日/小时桶）、存储占用排行、命名空间分布、未采集指标 |
| 审计日志 | 控制台全部写操作留痕（含失败尝试），按操作者/动作筛选 |
| 控制台账号 | 建号/删号/改角色/停用/重置口令（**真实生效**），最后一个超管保护 |
| 系统状态 | 控制台自身、数据库、云同步服务端的**实时探测**结果与能力矩阵 |

### 第二阶段交付（给主服务打补丁，把前一阶段做不了的两项打通）

| 能力 | 服务端改动 | 控制台改动 |
|---|---|---|
| **停用账号** | `users` 补 `status`/`disabled_at`/`disabled_reason` 三列；`login` 与 `authenticate` 两处拦截；`set_user_status`（改状态 + 清会话，单事务） | 停用/恢复端点 + 列表状态列 + 停用对话框（必填原因） |
| **流量统计** | 新模块 `metrics.rs`（内存聚合）+ 请求指标中间件 + 两张小时桶表 + 定期落库与保留期清理 | 流量序列/路由汇总/下载排行三个端点 + 完整的流量面板 |

### 仍然没做、且界面上写明了原因的

| 项 | 界面表现 | 卡在哪 |
|---|---|---|
| 插件下架/恢复 | 按钮**置灰** + 警告条 | 主服务市场索引是进程内 `RwLock` 缓存（`server/src/market.rs:69`），改库不刷新；插件包也是直接读盘返回 |
| 提审改判 | 列表页写明「`manual` 无处置入口，请让作者重新提交」 | 全仓没有任何端点能改判为通过 |
| 历史登录次数 | 「未采集」占位块 | 主服务登出即删会话行，登录事件没有留痕 |
| 独立访客 / IP 维度 | 「未采集」占位块 | frp 纯 TCP 透传下服务端看到的对端恒为回环地址 |

### 三种状态在图上必须看起来不同

这是整个流量面板最要紧的设计约束：

| 状态 | 画法 |
|---|---|
| 有请求 | 正常曲线 |
| 没有请求 | 贴地的 0 |
| **没有采集** | **断线 + 斜纹底纹**，并在说明里写明「服务端那时还没开始记录，不是没有流量」 |

把第三种画成第二种就是编数据。服务端是某次发版才开始采集的，在那之前的时段
一行都没有——`/api/stats/traffic` 的每个点因此都带 `covered` 标记，前端据此断线。

---

## 二、架构与关键文件

```text
  浏览器 ──HTTPS:7005──▶ itools-console ──┐
                                           ├──▶ MariaDB (itools 库)
  iTools 客户端 ─HTTPS:7101─▶ itools-sync ─┘
```

控制台**不代理**主服务流量，两者互不依赖（除数据库与一次只读的 `/health` 探测）。

### 后端（`console/src/`）

| 文件 | 职责 |
|---|---|
| `main.rs` | 启动、引导首个管理员、TLS 终止（手写 accept 循环，与主服务同一套）、过期会话回收 |
| `config.rs` | 全部配置（`CONSOLE_*` 前缀，与主服务 `SYNC_*` 隔离）。TLS 只配一个会**直接报错退出** |
| `auth.rs` | scrypt 口令（参数与 `server/src/auth.rs` 逐位一致，测试里钉了同一条 Node 向量）+ 令牌 sha256 |
| `ratelimit.rs` | 登录限流（固定窗口，纯内存） |
| `store/mod.rs` | 连接池、`console_*` 三张表 DDL、**主服务库能力探测**（`Caps`） |
| `store/admins.rs` | 控制台账号与会话（令牌存哈希、有 TTL、联表查角色故改角色立即生效） |
| `store/audit.rs` | 审计写入与查询 |
| `store/users.rs` | 终端用户查询 + 踢下线 + 删号。**没有任何 `SELECT v`，只有 `LENGTH(v)`** |
| `store/plugins.rs` | 市场与提审单查询，全只读 |
| `store/stats.rs` | 统计聚合。分桶**不依赖数据库时区**，在 SQL 里手算桶号 |
| `routes/mod.rs` | 路由组装、**统一鉴权守卫**、错误格式（带 `code`）、客户端 IP 中间件、安全响应头 |
| `routes/*.rs` | 各功能端点 |

### 第二阶段新增/改动的服务端文件（`server/src/`）

| 文件 | 改动 |
|---|---|
| `metrics.rs` | **新增**。内存聚合器：按 (小时, 路由模板, 方法, 状态类) 累计请求数/字节/耗时/延迟桶，另按插件名计下载量。带键数上限与丢弃计数（不静默） |
| `store.rs` | `users` 三列 + 两张指标表 + `SessionCheck` + `session_user_active` + `set_user_status` + 指标 upsert/清理 + `pool()` 访问器 |
| `routes.rs` | `login` 与 `authenticate` 两处停用拦截；`track_metrics` 中间件；`market_package` 记下载量；`AppState` 加 `metrics` |
| `config.rs` | `MetricsConfig`（开关 / flush 间隔 / 保留期） |
| `main.rs` | 挂中间件 + 落库任务（含按天清理超期数据） |

### 前端（`console/web/`）

零构建原生 ES module + 手写 SVG 图表，无框架无 CDN（CSP 只允许 `'self'`）。
改完 scp 即生效，秒级——不必为改一句文案重发 32MB 镜像。

`ui.js` 的 `h()` 工厂**对 `html` 属性直接抛错**，所有文本走 `textContent`：
用户名、插件描述、驳回原因都是外部输入，一个 innerHTML 就是存储型 XSS。

---

## 三、安全模型

- **账号完全隔离**：控制台账号与 iTools 云账号是两套主体，同名也不相干。
- **令牌不明文入库**：`console_sessions` 主键是令牌的 sha256。
  主服务那边是明文主键——一次拖库即拿到全部在线会话；控制台权限更高，这里收紧了。
- **会话有 TTL 且真的会回收**：默认 8 小时 + 每 10 分钟 GC。
  （主服务的 `sessions` 永不过期、只增不减，别重蹈。）
- **鉴权靠中间件不靠自觉**：受保护路由整组套 `middleware::from_fn`。
  主服务是每个 handler 手写三行 `authenticate`，漏一个就是完全公开的端点。
- **首次登录强制改密**：守卫在 `must_change_password` 为真时拦下除改密/登出/whoami 外的一切。
  放行登出是刻意的——否则被强制改密的账号连退出都做不到。
- **角色三档**：`super` / `admin` / `viewer`，未知角色按最小权限。
  删除/降级/停用**最后一个可用超管**会被拒绝（自锁保护）。
- **登录不泄露账号是否存在**：账号不存在时也走一次口令派生，耗时接近，
  返回文案与口令错误完全相同；但审计里如实区分「账号不存在 / 口令错误 / 账号已停用」。
- **审计含失败尝试**：`ok=0` 的行同样落库。
- **SQL 注入面**：`ORDER BY` 不能参数化，故排序字段走**白名单枚举**（`UserSort`），
  未知值回落默认；`LIKE` 的搜索词做 `escape_like` 转义（`%`/`_`/`\`）。
- **敏感值不出后端**：口令哈希与盐不进任何响应；数据库地址只以 `mysql://user:***@host` 形态出现。

---

## 四、验证结果（全部实测，非纸面）

### 1. 静态与单元
```
cargo check --all-targets   → 0 error 0 warning
cargo clippy --all-targets  → 0 warning
cargo test                  → 35 passed; 0 failed
```
单元测试覆盖：scrypt 与主服务向量一致性、令牌摘要、口令强度规则、
配置解析（TLS 成对校验/数值夹紧/口令 URL 编码/脱敏）、限流窗口与重置、
`LIKE` 转义、分页夹紧、角色权限、排序白名单、时间分桶与时区偏移、UTF-8 安全截断。

### 2. 本地端到端（真实 MariaDB + 真实 HTTP）
起 `mariadb:11` 容器，按 `server/src/store.rs` 的 DDL 建出主服务 5 张表并灌测试数据
（含中文用户名、已下架插件、`manual` 提审单等边界样本），**174 项断言全部通过**。

覆盖：公开端点不泄露配置、无令牌/伪造令牌 401、错口令不泄露账号存在性、
强制改密全链路（含旧令牌立即失效）、概览数字与库中数据逐一核对、
时间序列补齐与等距、`%`/`_` 搜索不被当通配符、恶意排序字段不注入、
分页夹紧、中文用户名往返、踢下线后会话归零、删号后查不到、
插件下架归属区分、`manual` 说明文案、只读角色的完整权限边界、
最后一个超管保护、审计各动作齐全、口令哈希不出响应、安全响应头、登录限流触发。

### 3. 浏览器真机（本地 + 生产）
逐页截图核对：登录、概览、用户、插件市场、提审单、数据统计、审计、控制台账号、系统状态；
亮/暗双主题；控制台无 JS 报错。

### 4. 生产环境（对外域名的 7005 端口）
**52 项断言全部通过**，且**全程只读**——没有删任何用户、没有踢任何会话、
没有改任何业务数据。唯一的写操作是控制台自己账号的首次强制改密。

实测数据：注册用户 5 · 现存会话 7 · 同步记录 22 · 上线插件 2（`chao-tool`、`deskbox`）· 提审 4 单。
能力探测结果 `market_entries.revoked_by=true`、`users.status=false`，与预期一致。
云同步服务端探测通过（`itools-sync`，41ms），主服务未受任何影响。

---

### 5. 第二阶段的验证

**主服务**：`cargo check`/`clippy` 0 warning、92 项单元测试、**12 项集成测试**（连真实 MariaDB）。
集成测试里 `disabled_account_is_blocked_on_every_path` 把停用的四条路径逐一钉死：
已有令牌失效、会话行被删、原口令重新登录被拒、口令错误时不泄露状态；
另有一条专门验证 `create_user` 的 upsert **不会**把停用状态洗掉。

**本地端到端**：两个服务一起跑（主服务 :7199 + 控制台 :7005，共用一个库），
**62 项断言**覆盖停用闭环（控制台写 → 主服务读）与流量采集（主服务记 → 控制台读），
包括「未采集的点不带数值而不是 0」「路由按模板归一而非具体插件名」
「延迟桶之和等于总请求数」这些容易被做歪的点。

**生产**：**38 项断言**。停用/恢复只作用于一个一次性探针账号 `__console_probe__`，
验证完立即删除；5 个真实用户全程 `active`、未受任何影响。
流量已在记录真实请求（Top 路由：`/health`、`/api/market/index`、`/*site`）。

**真机**：浏览器走了完整的停用流程（填原因 → 确认 → 状态变「已停用」→ 会话数归零 →
按钮变「恢复」），并核对了流量面板的斜纹未采集区间。

---

## 五、过程中发现并修复的问题

| # | 问题 | 修复 |
|---|---|---|
| 1 | `FLOOR()` 在 MariaDB 返回 DECIMAL，`r.get::<i64>` 直接 panic，表现为「连接被重置」这种毫无线索的故障 | 加 `CAST(... AS SIGNED)`，并把取值改成 `try_get`（类型不匹配时是可读的 500 而非 panic） |
| 2 | `ESCAPE '\'` 在 MariaDB 里反斜杠会把闭合引号吃掉 → SQL 语法错误，用户列表整页 500 | 写成 `ESCAPE '\\'`，5 处 |
| 3 | 部署脚本从 `run-new5.sh` 文本提取数据库口令，把结尾的单引号一起带进来 → 认证失败、容器重启循环 | 改从**正在运行的 `itools-server` 容器**的环境变量读，不受脚本书写格式影响 |
| 4 | 图表纵轴在峰值为 1 时画 4 条网格线，取整后显示成 `0,0,1,1,1` 一串重复数字 | 刻度数跟着量级收缩，计数类指标每格至少跨 1 |
| 5 | 服务端返回的说明文字里用了 Markdown `**`，而前端是纯文本渲染，星号原样显示 | 去掉 |
| 6 | 表格里成列的实心红删除按钮既刺眼又容易误点 | 改成默认红字、悬停才填充 |
| 7 | favicon 404、登录页副标题重复品牌名、顶栏筛选控件换行 | 内联 SVG favicon、副标题只留版本号、select 限宽 |

另有一条**排除掉的误判**：综合勘察报告称「服务端当前编译不过（`routes.rs:744` 调用与
`set_revoked` 新签名参数数量不符）」。实测 `routes.rs` 已跟上 4 参签名，该结论不成立——
基线是完整的。

### 第二阶段修复的问题

| # | 问题 | 修复 |
|---|---|---|
| 8 | `disabled_reason TEXT NOT NULL` **没有默认值**——TEXT 在 MySQL 上不允许 `DEFAULT`，于是每条 INSERT 都必须显式给值，而 `create_user` 只列了最初那四列 → **登录直接 500** | 改成 `VARCHAR(500) NOT NULL DEFAULT ''`。这个 bug 是集成测试抓到的，纯靠人眼审 DDL 很难发现 |
| 9 | 停用后旧令牌返回 401 而非预期的 403 | 不是 bug 是设计：停用**连会话一起删**（主路径），`authenticate` 的状态复查是**兜底**。测试改成分别验证这两层，并另插一条残留会话验证兜底确实返回 403 + 原因 |
| 10 | 部署时用 `sed` 往启动脚本插多行，`\n` 没被解释成换行，脚本被写坏 | 发现那三个指标配置项的默认值（true/60/180）就是要的值，**根本不用显式写**——启动脚本只换镜像 tag，`diff` 确认只有一行不同 |

---

## 六、上线拓扑与运维

| 项 | 值 |
|---|---|
| 容器 | `itools-console`（当前镜像 `itools-console:v3`，32MB） |
| 云同步服务端 | `itools-server`（当前镜像 `itools-sync-server:rust-new6`，含停用位与指标采集） |
| 网络 | host 网络，绑 `127.0.0.1:7005`，本进程终结 TLS |
| 证书 | NAS 上的证书目录（与云同步服务端共用同一份），部署时由 `NAS_CERT_DIR` 指定 |
| 数据库 | `127.0.0.1:3306` 的 `itools` 库（群晖 MariaDB 套件，与主服务同库） |
| 前端 | `/volume1/docker/itools-data/console-web` 挂载到 `/app/web`（改前端 scp 即生效） |
| 启动脚本 | `/volume1/docker/itools-build/run-console.sh`（700 权限，运行时从主服务容器读库口令） |
| 外网 | frp 隧道 `itools-console-7005`，由**独立容器 `itools-frpc`** 提供 |

### 关于那个独立的 frpc 实例

主 `frpc.toml` 属 `root:root`、本账号不可写，而本机的 `sudo` **只对 docker 免密**
（`synopkg restart frpc` 之类根本执行不了）。更要命的是：**SSH 端口本身就是
frp 隧道转发的**——重启 frpc 若因配置有误起不来，就再也连不上这台机器了。

因此没有动主配置，而是用 NAS 上现成的 frpc 二进制起了第二个实例（容器 `itools-frpc`，
`--restart unless-stopped`，配置 `/volume1/docker/itools-build/frpc-console.toml`，
只含 7005 一条隧道，全局段从主配置复制以复用同一个 frps token）。
现有隧道**一条都没碰**，零风险。

> ⚠ 该容器借用 `itools-console:v1` 镜像**只是为了拿一个能跑 frpc 二进制的 glibc 环境**
> （NAS 上没有独立的 debian slim 镜像，不想为此再拉一个）。清理旧镜像时别把 v1 删了，
> 或者先把这个容器重建到当前 tag。

> **建议的长期做法**：把 7005 那条 `[[proxies]]` 并进主 `frpc.toml`，再停掉 `itools-frpc` 容器。
> 需要 root，留给方便时做。在那之前这个额外容器是必需的，别误删。

> 部署时需 `export NAS_CERT_DIR=<证书目录>`（脚本里没有默认值——公开仓库不留运维拓扑）。
> 本环境该用哪个目录，看 NAS 上 `run-new5.sh` 里挂到 `/certs` 的那个卷。

### 更新方式

- 改后端 → `./deploy-to-nas.sh v2`（重新构建 + 传 32MB 镜像 + 换容器，约一分钟中断）
- 改前端 → 只需 scp `web/` 到 `$NAS_CONSOLE_WEB_DIR`，秒级、无中断

---

## 七、有意保留的限制项

1. **frp TCP 透传下拿不到真实客户端 IP**：服务端看到的对端恒为回环地址，
   `CONSOLE_TRUST_PROXY` 也无 XFF 可用。后果有两个，都需要知情：
   登录限流实际是**全局闸门**（一个人被限流，所有人都进不来），审计日志里的 IP 也只是回环地址。
   要真实 IP 需要 frp 改 HTTP 模式或前置反代注入 XFF。
2. **用户列表的聚合是现算的**（子查询全表扫 `data_records`）。当前几十个账号毫无压力；
   涨到十万级要改物化统计表，且应由主服务在写入时维护。
3. **首次建表在群晖上慢**（实测 1.5–3.4 秒/张，触发 sqlx 的 slow statement 警告）。
   只发生在第一次启动。
4. **控制台自身的访问量也没有采集**——它自己也没装指标中间件。第二阶段一并做。

---

## 八、第二阶段（已完成）

两个补丁都已实现、上线、验证。要点如下。

### 1. `users.status` 停用位（已完成）
四处缺一不可，少任何一处都是「看着能用、点了不生效」：
- `server/src/store.rs`：`users` 加 `status` / `disabled_at` / `disabled_reason`
  （沿用已有的 `MIGRATIONS` 补列机制，先查 `information_schema` 再 `ALTER`）。
  生产上线时三列迁移逐条执行成功，日志可查。
- `server/src/routes.rs::login`：**口令正确之后**才判停用，返回 403 + 原因。
  顺序很重要——先判停用的话，这个接口就成了「查某账号是否被封」的探测器；
  口令不对仍是 401，不泄露账号状态。
- `server/src/routes.rs::authenticate`：复查停用位（全服唯一鉴权收口），
  挡住任何因竞态残留的会话，返回 403 + 原因。
- `set_user_status`：改状态与清会话在**同一个事务**里。
控制台侧：`Caps::users_status` 探测到该列后，`/api/meta` 与 `/api/whoami` 的 `capabilities`
翻成 `true`，前端的停用开关据此启用；探测不到时开关置灰、调 API 返回 501。

**仍待做**：客户端侧（`src/admin/account.ts` / `src-tauri/src/account.rs`）目前把 403
和 401 一样处理，被停用的用户在客户端会看到「未登录」而不是真实原因。
服务端已经把原因放进了响应体，客户端接一下就能显示——这是第三阶段的一个小项。

### 2. 请求指标采集（已完成）
- `metrics.rs`：内存聚合，键是 (小时桶, 路由模板, 方法, 状态码大类)，
  累计请求数 / 进出字节 / 耗时总和与最大值 / 四个延迟桶
- `track_metrics` 中间件挂在整个 Router 上，官网静态站与 `/download` 也计入
- 每 `SYNC_METRICS_FLUSH_SEC`（默认 60）秒 `ON DUPLICATE KEY UPDATE ... + VALUES(...)`
  **累加**落库；按天清理超出 `SYNC_METRICS_RETENTION_DAYS`（默认 180）的数据

几条刻意的取舍，都写进了 `server/README.md`：
- **路由模板不是具体路径**（`/api/market/package/{name}`、`/download/*`、`/*site`），
  否则基数会随插件数、版本数无限增长
- **不算 P95**：只给四个延迟桶的精确计数与 avg/max。从桶插值出的百分位是估算，
  与其给一个看着精确其实是猜的数，不如给精确的分布
- **字节数取 body 的确定长度**，分块传输记 0：少记而不是猜
- 不记 IP、不记 UA、不记请求体内容

### 3. 插件处置（第三阶段候选）
主服务补一个下架端点或索引刷新钩子后，控制台的下架按钮才有意义。
`manual` 状态的人工改判要先想清楚审计与责任归属——它绕过了 LLM 审核这道安全闸。

---

## 九、给运维的第一件事

初始超管 `jimhy` 的口令**已由我在验证时改过一次**（见交付说明中的临时口令）。
请登录后立即在「控制台账号 → 重置口令」改成自己的，或者直接删掉这个账号新建一个。
初始引导用的环境变量已从容器里清除，`docker inspect` 看不到任何口令。
