{
  "name": "itools",
  "version": "1.4.1",
  "description": "iTools — macOS 风格的效率启动器（Tauri 2 + Rust + HTML）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && npm run check:contract && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "tsc --noEmit && npm run check:contract",
    "check:contract": "node scripts/check-contract.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
